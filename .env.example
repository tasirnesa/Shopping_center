# Docker Compose local configuration. Copy this file to `.env` and replace the
# JWT values before sharing or deploying the application.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456
POSTGRES_DB=shopping_center
JWT_SECRET=change-this-before-production
JWT_REFRESH_SECRET=change-this-before-production
